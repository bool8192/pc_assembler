# Необходимые библиотеки для AI-агента

# Основной фреймворк для создания агентов
smolagents==1.24
smolagents[openai]==1.24

# Для работы с переменными окружения
python-dotenv==1.0.0

# Дополнительные зависимости (если потребуются)
requests>=2.32.3

psycopg2-binary
